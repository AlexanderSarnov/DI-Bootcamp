// Exercise 3: Union Types

// Instructions
// Use union types to declare a variable id that can be either a string or a number.

// Declaration of a union type variable id, that can hold either a string or a number with an empty string assigned as the value.
const id: string | number = '';
