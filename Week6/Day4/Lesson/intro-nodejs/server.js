const a = require('./app'); // here we take all the functions from the app.js file and assign them to the variable a.

console.log(a.hello('Dan')); // Hello, Dan! Using the dot notation to access the hello function from app.js
