export function greeting(name) {
  console.log(`Hello ${name}, weclome to NodeJS ES6 Modules`);
};
/**
* this effectively allows to export  a function from greeting.js file
* using th ES6 syntax for async export/import operations
* which is a nowdays preferred way.
* It requires the package.json file to be created (how?)
* terminal > npm init -y
*/
