// define a greeting function
const greeting = function (name) {
  console.log(`Hello ${name}, welcome to NodeJS`);
};

module.exports = greeting;
// allows to use function greetig from another js file (module)
